# serverless-puppeteer-sample

install node.

```
npm install -g serverless
npm install
npm run test
```

```
> project@1.0.0 test /path/to/project
> serverless invoke local --function hello -p event.json

"Example Domain"
```
